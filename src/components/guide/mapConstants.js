export const TEKAPO_CENTER = [-44.0068, 170.4779]
export const DEFAULT_ZOOM = 13
export const FOCUS_ZOOM = 15

// Height (px) of BottomDrawer's collapsed peek — just its grab-handle/label
// row, nothing else. Shared with FloatingButtons (which sits just above that
// row on mobile) so the two can never drift out of sync.
export const MOBILE_SHEET_COLLAPSED_PEEK_PX = 64
